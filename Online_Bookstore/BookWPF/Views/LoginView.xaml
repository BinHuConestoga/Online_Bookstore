<Window x:Class="OnlineBookstore.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:OnlineBookstore.ViewModels"
        Title="Login" Height="350" Width="600" WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:LoginViewModel />
    </Window.DataContext>
    <Grid Background="#2C3E50">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Welcome to Online Bookstore" Foreground="White" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center" />

        <StackPanel Grid.Row="1" Margin="0,10" HorizontalAlignment="Center">
            <TextBox Width="300" Height="35" Margin="0,10" PlaceholderText="Username" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />
            <PasswordBox Width="300" Height="35" Margin="0,10" Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Login" Width="150" Height="40" Margin="0,20" Command="{Binding LoginCommand}" />
        </StackPanel>

        <TextBlock Text="Enter your credentials" Foreground="White" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Bottom" Grid.Row="2" />
    </Grid>
</Window>
